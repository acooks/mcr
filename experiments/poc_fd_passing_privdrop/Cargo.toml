[package]
name = "poc_fd_passing_privdrop"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
libc = "0.2"
nix = { version = "0.30.1", features = ["net", "socket", "user", "process", "uio"] }
