[package]
name = "poc_helper_socket_igmp"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
libc = "0.2"
socket2 = { version = "0.6.1", features = ["all"] }
nix = { version = "0.30.1", features = ["net", "socket", "poll"] }
