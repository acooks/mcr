[package]
name = "poc_helper_socket_igmp"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
libc = "0.2"
socket2 = { version = "0.5", features = ["all"] }
nix = { version = "0.30", features = ["net", "socket", "poll"] }
